---
phase: 07-cli-wiring-and-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/converter/converter_test.go
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - CLI-06
  - OUT-04

must_haves:
  truths:
    - "san-conv mds2brocade basic.cfg runs end-to-end and writes alicreate/zonecreate/cfgcreate to stdout"
    - "san-conv mds2brocade basic.cfg --output out.txt writes FOS commands to out.txt not stdout"
    - "san-conv mds2brocade basic.cfg --script out.sh writes an executable shell script with defzone preamble"
    - "san-conv brocade2mds cfgshow_basic.cfg writes device-alias and zone blocks to stdout"
    - "After conversion, stderr contains 'Summary:' with alias/zone/config/warning counts"
    - "san-conv mds2brocade missing.txt exits non-zero; conversion with warnings-only exits 0"
    - "--fos-version 8.1+ is accepted and passed through to the sanitizer"
  artifacts:
    - path: "internal/converter/converter_test.go"
      provides: "Integration tests for converter.Run() covering all CLI requirements"
      min_lines: 80
  key_links:
    - from: "internal/converter/converter_test.go"
      to: "internal/converter/converter.go"
      via: "converter.Run() call"
      pattern: "Run\\(opts"
---

<objective>
Write a failing integration test file for the converter package. Every test calls
`converter.Run(opts, stdout, stderr)` against real fixture files in `testdata/`.

Purpose: Establish the RED phase of TDD — tests encode the full behavioral contract
before any implementation exists. The tests MUST fail with "undefined: Run" when
`converter.go` does not exist.

Output: `internal/converter/converter_test.go` — fails to compile until Plan 07-02
creates `converter.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cli-wiring-and-integration/07-RESEARCH.md

<interfaces>
<!-- All signatures are verified from source. No exploration needed. -->

From internal/emitter/brocade/emitter.go:
```go
func Emit(cfg *ir.ZoningConfig, w io.Writer, scriptMode bool) error
```

From internal/emitter/mds/emitter.go:
```go
func Emit(cfg *ir.ZoningConfig, w io.Writer) error
```

From internal/parser/mds/parser.go:
```go
func Parse(r io.Reader) (*ir.ZoningConfig, error)
```

From internal/parser/brocade/parser.go:
```go
func Parse(r io.Reader) (*ir.ZoningConfig, error)
```

From internal/validator/sanitizer.go:
```go
func Sanitize(cfg *ir.ZoningConfig, fosVersion string) *ir.ZoningConfig
```

Target interface to be created in Plan 07-02 (internal/converter/converter.go):
```go
package converter

type Options struct {
    InputFile  string // path to input config file (CLI-01)
    Direction  string // "mds2brocade" or "brocade2mds" (CLI-02)
    OutputFile string // "" means stdout (CLI-03)
    ScriptFile string // "" means no script file (CLI-04)
    FOSVersion string // "pre-8.1" or "8.1+" (CLI-05)
}

func Run(opts Options, stdout io.Writer, stderr io.Writer) error
```

Test fixture paths (relative to package root internal/converter/):
- ../../testdata/mds/basic.cfg         (has device-alias, zones, zoneset)
- ../../testdata/mds/edge_cases.cfg    (edge cases including orphan zones)
- ../../testdata/brocade/cfgshow_basic.cfg  (cfgshow format)
- ../../testdata/brocade/cli_basic.cfg      (CLI script format)
</interfaces>
</context>

<feature>
  <name>converter.Run integration tests (TDD red phase)</name>
  <files>internal/converter/converter_test.go</files>
  <behavior>
    Test 1 — mds2brocade basic stdout: Run with InputFile=testdata/mds/basic.cfg, Direction=mds2brocade, FOSVersion=pre-8.1. Expect no error, stdout contains "alicreate", stdout contains "zonecreate", stderr contains "Summary:".

    Test 2 — mds2brocade output file: Run with OutputFile=t.TempDir()+"/out.txt". Expect no error, out.txt exists and contains "alicreate", stdout is EMPTY (nothing written to stdout).

    Test 3 — mds2brocade script file: Run with ScriptFile=t.TempDir()+"/out.sh". Expect no error, out.sh exists, out.sh contains "defzone --noaccess", out.sh contains "cfgsave". Verify out.sh is executable (os.Stat mode &0111 != 0).

    Test 4 — mds2brocade script does NOT double-count warnings in stderr summary: Run with a fixture that causes sanitizer warnings. The Summary: line on stderr must appear exactly once.

    Test 5 — brocade2mds basic stdout: Run with InputFile=testdata/brocade/cfgshow_basic.cfg, Direction=brocade2mds. Expect no error, stdout contains "device-alias database", stdout contains "zone name".

    Test 6 — brocade2mds sanitizer NOT applied: Brocade names may contain hyphens (valid in NX-OS). Run brocade2mds direction with a fixture that has a hyphenated alias name. Verify the hyphen survives in stdout (NOT replaced with underscore).

    Test 7 — fos-version flag accepted: Run mds2brocade with FOSVersion="8.1+". Expect no error (flag is accepted and passed to sanitizer without crashing).

    Test 8 — missing input file returns error: Run with InputFile="nonexistent_file_xyz.cfg". Expect error != nil (CLI-06 fatal IO error path).

    Test 9 — unknown direction returns error: Run with Direction="sideways". Expect error != nil.

    Test 10 — stderr summary format: The Summary: line must match the pattern "Summary: N aliases, N zones, N configs converted; N warnings" where N are non-negative integers.
  </behavior>
  <implementation>
    Write tests only — no converter.go yet. Use:
    - `package converter` (same package for white-box access when converter.go exists)
    - `bytes.Buffer` for stdout and stderr capture
    - `require.NoError` / `require.Error` from testify
    - `t.Parallel()` on every test
    - `os.Stat` to verify file existence and executable permission (mode & 0o111)
    - `require.Contains` for content assertions
    - `require.Regexp` for the summary line format check

    All tests must compile once converter.go defines `Options` and `Run`. Until then,
    `go test ./internal/converter/...` must fail with: `undefined: Options` or `undefined: Run`.
  </implementation>
</feature>

<verification>
<automated>cd /Users/fjacquet/Projects/san-conv && go build ./internal/converter/... 2>&1 | grep -c "undefined\|cannot find" || true</automated>

Expected: build fails with "undefined" errors (Run/Options do not exist yet).
After Plan 07-02: `go test ./internal/converter/... -v -count=1` passes all 10 tests.
</verification>

<success_criteria>
- internal/converter/converter_test.go exists with at least 10 table-driven or named tests
- File references converter.Options and converter.Run (which don't exist yet)
- `go vet ./internal/converter/...` fails with "undefined: Run" or "undefined: Options"
- Every test has require.NoError or require.Error assertions (no assertion-free tests)
- Tests 2 and 3 use t.TempDir() for output file paths (no hardcoded temp paths)
- Test 3 asserts out.sh has executable permission via os.Stat
- Test 10 asserts stderr summary matches format string with regexp
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-wiring-and-integration/07-01-SUMMARY.md`
</output>
