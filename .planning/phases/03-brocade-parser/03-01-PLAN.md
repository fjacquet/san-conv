---
phase: 03-brocade-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/brocade/cfgshow_basic.cfg
  - testdata/brocade/cfgshow_continuation.cfg
  - testdata/brocade/cli_basic.cfg
  - testdata/brocade/cli_pwwn_members.cfg
  - testdata/brocade/edge_cases.cfg
  - internal/parser/brocade/parser_test.go
autonomous: true
requirements:
  - PARSE-07
  - PARSE-08
  - PARSE-09

must_haves:
  truths:
    - "Five Brocade test fixture files exist covering cfgshow basic, cfgshow continuation, CLI basic, CLI pWWN members, and edge cases"
    - "parser_test.go contains table-driven tests for all five fixtures with assertions against expected IR contents"
    - "Tests reference the brocade.Parse function and will fail until parser.go is implemented (TDD red phase)"
  artifacts:
    - path: "testdata/brocade/cfgshow_basic.cfg"
      provides: "Basic cfgshow fixture with alias, zone, cfg"
      contains: "Defined configuration:"
    - path: "testdata/brocade/cfgshow_continuation.cfg"
      provides: "Backslash continuation fixture"
      contains: "\\$"
    - path: "testdata/brocade/cli_basic.cfg"
      provides: "Basic FOS CLI script fixture"
      contains: "alicreate"
    - path: "testdata/brocade/cli_pwwn_members.cfg"
      provides: "CLI fixture with raw pWWN zone members"
      contains: "zonecreate"
    - path: "testdata/brocade/edge_cases.cfg"
      provides: "Edge case fixture: empty zone, Effective section boundary"
      contains: "Effective configuration:"
    - path: "internal/parser/brocade/parser_test.go"
      provides: "Table-driven tests for Brocade parser"
      contains: "func TestParse"
  key_links:
    - from: "internal/parser/brocade/parser_test.go"
      to: "testdata/brocade/*.cfg"
      via: "os.Open with filepath.Join"
      pattern: 'filepath\.Join.*testdata.*brocade'
    - from: "internal/parser/brocade/parser_test.go"
      to: "internal/parser/brocade/parser.go"
      via: "Parse(f) function call"
      pattern: "Parse\\(f\\)"
---

<objective>
Create five Brocade test fixture files and the parser_test.go file with table-driven tests for all fixtures. This is the TDD red phase -- tests will compile but fail because parser.go does not exist yet (only doc.go).

Purpose: Establish the test contract before implementation. Every fixture exercises a specific requirement (PARSE-07 cfgshow, PARSE-08 CLI, PARSE-09 auto-detection). Tests define the expected IR output shape, member counts, and edge case behavior that the parser must satisfy.

Output: Five `.cfg` fixture files in `testdata/brocade/` and one `parser_test.go` file in `internal/parser/brocade/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brocade-parser/03-RESEARCH.md

<interfaces>
<!-- IR struct definitions the test must assert against -->

From internal/ir/zoningconfig.go:
```go
type ZoningConfig struct {
    Aliases      map[string]*Alias
    Zones        map[string]*Zone
    ZoneConfigs  map[string]*ZoneConfig
    SourceFormat string
    Warnings     []string
}

type Alias struct {
    Name string
    PWWN string
    VSAN int
}

type Zone struct {
    Name    string
    VSAN    int
    Members []*ZoneMember
}

type ZoneMember struct {
    Type  string // "pwwn", "alias", "unsupported"
    Value string
}

type ZoneConfig struct {
    Name      string
    VSAN      int
    ZoneNames []string
}
```

From internal/parser/mds/parser_test.go (reference test structure):
```go
func TestParse(t *testing.T) {
    t.Parallel()
    tests := []struct {
        name    string
        fixture string
        checkFn func(t *testing.T, cfg *ir.ZoningConfig)
    }{ /* ... */ }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            t.Parallel()
            fixturePath := filepath.Join("..", "..", "..", "testdata", "mds", tt.fixture)
            f, err := os.Open(fixturePath)
            require.NoError(t, err, "failed to open fixture %s", tt.fixture)
            defer f.Close() //nolint:errcheck
            cfg, err := Parse(f)
            require.NoError(t, err)
            require.NotNil(t, cfg)
            require.Equal(t, "mds-nxos", cfg.SourceFormat)
            tt.checkFn(t, cfg)
        })
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create five Brocade test fixture files</name>
  <files>
    testdata/brocade/cfgshow_basic.cfg,
    testdata/brocade/cfgshow_continuation.cfg,
    testdata/brocade/cli_basic.cfg,
    testdata/brocade/cli_pwwn_members.cfg,
    testdata/brocade/edge_cases.cfg
  </files>
  <read_first>
    testdata/brocade/.gitkeep,
    testdata/mds/basic.cfg,
    .planning/phases/03-brocade-parser/03-RESEARCH.md
  </read_first>
  <action>
Create five fixture files in `testdata/brocade/`. Remove `.gitkeep` after creating the real files. Each fixture exercises a specific requirement per PARSE-07, PARSE-08, PARSE-09.

**File 1: `testdata/brocade/cfgshow_basic.cfg`** (PARSE-07 basic, PARSE-09 auto-detect cfgshow)
```
Defined configuration:
 cfg:   Production_cfg
                fabric_zone1; fabric_zone2
 zone:  fabric_zone1
                host_01; storage_01
 zone:  fabric_zone2
                host_02; storage_02
 alias: host_01
                10:00:00:00:c9:ab:cd:ef
 alias: storage_01
                50:05:07:61:01:23:45:67
 alias: host_02
                10:00:00:00:c9:ab:cd:ee
 alias: storage_02
                50:05:07:61:01:23:45:68
```
Expected IR:
- 4 aliases (host_01, storage_01, host_02, storage_02), each VSAN=0
- 2 zones (fabric_zone1 with 2 alias members, fabric_zone2 with 2 alias members), each VSAN=0
- 1 ZoneConfig (Production_cfg with ZoneNames=[fabric_zone1, fabric_zone2]), VSAN=0
- SourceFormat="brocade-fos", 0 warnings

**File 2: `testdata/brocade/cfgshow_continuation.cfg`** (PARSE-07 backslash continuation)
```
Defined configuration:
 cfg:   BigFabric_cfg
                big_zone; small_zone
 zone:  big_zone
                member_01; member_02; \
                member_03; member_04
 zone:  small_zone
                only_member
 alias: member_01
                10:00:00:00:c9:00:00:01
 alias: member_02
                10:00:00:00:c9:00:00:02
 alias: member_03
                10:00:00:00:c9:00:00:03
 alias: member_04
                10:00:00:00:c9:00:00:04
 alias: only_member
                10:00:00:00:c9:00:00:05
```
Expected IR:
- 5 aliases
- 2 zones: big_zone with 4 members (NOT 2 -- continuation must be handled), small_zone with 1 member
- 1 ZoneConfig (BigFabric_cfg with ZoneNames=[big_zone, small_zone])
- 0 warnings

**File 3: `testdata/brocade/cli_basic.cfg`** (PARSE-08 basic, PARSE-09 auto-detect CLI)
```
alicreate "host_01", "10:00:00:00:c9:ab:cd:ef"
alicreate "storage_01", "50:05:07:61:01:23:45:67"
zonecreate "fabric_zone1", "host_01;storage_01"
cfgcreate "Production_cfg", "fabric_zone1"
```
Expected IR:
- 2 aliases (host_01, storage_01), VSAN=0
- 1 zone (fabric_zone1 with 2 alias members), VSAN=0
- 1 ZoneConfig (Production_cfg with ZoneNames=[fabric_zone1]), VSAN=0
- SourceFormat="brocade-fos", 0 warnings

**File 4: `testdata/brocade/cli_pwwn_members.cfg`** (PARSE-08 pWWN members in zonecreate)
```
zonecreate "direct_zone", "10:00:00:00:c9:ab:cd:ef;50:05:07:61:01:23:45:67"
cfgcreate "Direct_cfg", "direct_zone"
```
Expected IR:
- 0 aliases
- 1 zone (direct_zone with 2 pwwn members), VSAN=0
- 1 ZoneConfig (Direct_cfg with ZoneNames=[direct_zone])
- 0 warnings

**File 5: `testdata/brocade/edge_cases.cfg`** (edge cases: empty zone, Effective section boundary)
```
Defined configuration:
 cfg:   Test_cfg
                empty_zone; real_zone
 zone:  empty_zone
 zone:  real_zone
                host_a
 alias: host_a
                10:00:00:00:c9:ff:ff:01

Effective configuration:
 cfg:   Test_cfg
                empty_zone; real_zone
```
Expected IR:
- 1 alias (host_a)
- 2 zones: empty_zone with 0 members, real_zone with 1 alias member
- 1 ZoneConfig (Test_cfg with ZoneNames=[empty_zone, real_zone])
- 0 warnings
- CRITICAL: No duplicated entries from the Effective section
  </action>
  <verify>
    <automated>ls -la /Users/fjacquet/Projects/san-conv/testdata/brocade/*.cfg | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - testdata/brocade/cfgshow_basic.cfg contains "Defined configuration:" on line 1
    - testdata/brocade/cfgshow_basic.cfg contains " alias: host_01"
    - testdata/brocade/cfgshow_continuation.cfg contains a line ending with " \" (backslash continuation)
    - testdata/brocade/cfgshow_continuation.cfg contains "member_03; member_04" on a continuation line
    - testdata/brocade/cli_basic.cfg contains 'alicreate "host_01"'
    - testdata/brocade/cli_basic.cfg contains 'cfgcreate "Production_cfg"'
    - testdata/brocade/cli_pwwn_members.cfg contains 'zonecreate "direct_zone"'
    - testdata/brocade/cli_pwwn_members.cfg contains "10:00:00:00:c9:ab:cd:ef;50:05:07:61:01:23:45:67"
    - testdata/brocade/edge_cases.cfg contains "Effective configuration:" (section boundary)
    - testdata/brocade/edge_cases.cfg contains " zone:  empty_zone" followed by " zone:  real_zone" (empty zone has no member lines between them)
    - Five .cfg files exist in testdata/brocade/ (count = 5)
    - .gitkeep is removed from testdata/brocade/
  </acceptance_criteria>
  <done>Five Brocade fixture files exist with correct content covering cfgshow basic, cfgshow continuation, CLI basic, CLI pWWN members, and edge cases. .gitkeep removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create parser_test.go with table-driven tests for all five fixtures</name>
  <files>internal/parser/brocade/parser_test.go</files>
  <read_first>
    internal/parser/mds/parser_test.go,
    internal/ir/zoningconfig.go,
    internal/parser/brocade/doc.go,
    testdata/brocade/cfgshow_basic.cfg,
    testdata/brocade/cfgshow_continuation.cfg,
    testdata/brocade/cli_basic.cfg,
    testdata/brocade/cli_pwwn_members.cfg,
    testdata/brocade/edge_cases.cfg
  </read_first>
  <action>
Create `internal/parser/brocade/parser_test.go` following the exact structure of `internal/parser/mds/parser_test.go`. Use package `brocade`, import `github.com/fjacquet/san-conv/internal/ir` and `github.com/stretchr/testify/require`.

The test file must:

1. Use `t.Parallel()` at both the top level and inside each subtest
2. Use `filepath.Join("..", "..", "..", "testdata", "brocade", tt.fixture)` for fixture paths
3. Call `Parse(f)` (the function that does not exist yet -- this is TDD red phase)
4. Assert `cfg.SourceFormat == "brocade-fos"` for every test case
5. Use `require` (not `assert`) for all assertions -- stops on first failure per project convention

**Test cases (5 subtests in the table):**

**Case 1: "cfgshow basic with aliases zones and cfg"**
- Fixture: `cfgshow_basic.cfg`
- Assert: `len(cfg.Aliases) == 4` (host_01, storage_01, host_02, storage_02)
- Assert: `cfg.Aliases["host_01"].PWWN == "10:00:00:00:c9:ab:cd:ef"`
- Assert: `cfg.Aliases["host_01"].VSAN == 0`
- Assert: `cfg.Aliases["storage_01"].PWWN == "50:05:07:61:01:23:45:67"`
- Assert: `len(cfg.Zones) == 2` (fabric_zone1, fabric_zone2)
- Assert: `cfg.Zones["fabric_zone1"]` exists, VSAN==0, 2 members, both type "alias"
- Assert: `cfg.Zones["fabric_zone1"].Members[0].Value == "host_01"`
- Assert: `cfg.Zones["fabric_zone1"].Members[1].Value == "storage_01"`
- Assert: `len(cfg.ZoneConfigs) == 1`
- Assert: `cfg.ZoneConfigs["Production_cfg"]` exists, VSAN==0, ZoneNames==["fabric_zone1", "fabric_zone2"]
- Assert: `len(cfg.Warnings) == 0`

**Case 2: "cfgshow backslash continuation"**
- Fixture: `cfgshow_continuation.cfg`
- Assert: `len(cfg.Aliases) == 5`
- Assert: `len(cfg.Zones) == 2`
- Assert: `cfg.Zones["big_zone"]` has exactly 4 members (NOT 2 -- this is the critical continuation test)
- Assert: `cfg.Zones["big_zone"].Members[0].Value == "member_01"`
- Assert: `cfg.Zones["big_zone"].Members[1].Value == "member_02"`
- Assert: `cfg.Zones["big_zone"].Members[2].Value == "member_03"`
- Assert: `cfg.Zones["big_zone"].Members[3].Value == "member_04"`
- Assert: `cfg.Zones["small_zone"]` has exactly 1 member
- Assert: `cfg.ZoneConfigs["BigFabric_cfg"]` has ZoneNames==["big_zone", "small_zone"]
- Assert: `len(cfg.Warnings) == 0`

**Case 3: "CLI basic with alicreate zonecreate cfgcreate"**
- Fixture: `cli_basic.cfg`
- Assert: `len(cfg.Aliases) == 2`
- Assert: `cfg.Aliases["host_01"].PWWN == "10:00:00:00:c9:ab:cd:ef"`
- Assert: `len(cfg.Zones) == 1`
- Assert: `cfg.Zones["fabric_zone1"]` has 2 members, both type "alias"
- Assert: `cfg.Zones["fabric_zone1"].Members[0].Value == "host_01"`
- Assert: `cfg.Zones["fabric_zone1"].Members[1].Value == "storage_01"`
- Assert: `len(cfg.ZoneConfigs) == 1`
- Assert: `cfg.ZoneConfigs["Production_cfg"]` has ZoneNames==["fabric_zone1"]
- Assert: `len(cfg.Warnings) == 0`

**Case 4: "CLI pWWN members in zonecreate"**
- Fixture: `cli_pwwn_members.cfg`
- Assert: `len(cfg.Aliases) == 0`
- Assert: `len(cfg.Zones) == 1`
- Assert: `cfg.Zones["direct_zone"]` has 2 members, both type "pwwn"
- Assert: `cfg.Zones["direct_zone"].Members[0].Value == "10:00:00:00:c9:ab:cd:ef"`
- Assert: `cfg.Zones["direct_zone"].Members[1].Value == "50:05:07:61:01:23:45:67"`
- Assert: `len(cfg.ZoneConfigs) == 1`
- Assert: `len(cfg.Warnings) == 0`

**Case 5: "edge cases empty zone and Effective section boundary"**
- Fixture: `edge_cases.cfg`
- Assert: `len(cfg.Aliases) == 1`
- Assert: `cfg.Aliases["host_a"].PWWN == "10:00:00:00:c9:ff:ff:01"`
- Assert: `len(cfg.Zones) == 2`
- Assert: `cfg.Zones["empty_zone"]` exists, Members is empty (len==0)
- Assert: `cfg.Zones["real_zone"]` exists, 1 member, type "alias", value "host_a"
- Assert: `len(cfg.ZoneConfigs) == 1`
- Assert: `cfg.ZoneConfigs["Test_cfg"]` has ZoneNames==["empty_zone", "real_zone"]
- Assert: `len(cfg.Warnings) == 0`
- CRITICAL: assert len(cfg.Aliases)==1, len(cfg.Zones)==2, len(cfg.ZoneConfigs)==1 to confirm Effective section was NOT parsed (would double counts)

NOTE: This file will NOT compile yet because `Parse` is not defined in the brocade package. This is expected (TDD red phase). The executor should verify the test file is syntactically valid Go by running `go vet ./internal/parser/brocade/` which will fail with "undefined: Parse" -- confirming the test references the right function name.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/san-conv && go vet ./internal/parser/brocade/ 2>&1 | grep -c "undefined: Parse"</automated>
  </verify>
  <acceptance_criteria>
    - internal/parser/brocade/parser_test.go exists
    - parser_test.go contains "package brocade"
    - parser_test.go contains "func TestParse(t *testing.T)"
    - parser_test.go contains 't.Parallel()' at top level
    - parser_test.go contains 'require.Equal(t, "brocade-fos", cfg.SourceFormat'
    - parser_test.go contains "cfgshow_basic.cfg" fixture reference
    - parser_test.go contains "cfgshow_continuation.cfg" fixture reference
    - parser_test.go contains "cli_basic.cfg" fixture reference
    - parser_test.go contains "cli_pwwn_members.cfg" fixture reference
    - parser_test.go contains "edge_cases.cfg" fixture reference
    - parser_test.go uses require (not assert) for all test assertions
    - parser_test.go contains assertion for big_zone having 4 members (continuation test)
    - parser_test.go contains assertion for empty_zone having 0 members
    - parser_test.go imports "github.com/stretchr/testify/require"
    - parser_test.go imports "github.com/fjacquet/san-conv/internal/ir"
    - go vet reports "undefined: Parse" (confirms TDD red phase -- function not yet implemented)
  </acceptance_criteria>
  <done>parser_test.go exists with 5 table-driven test cases covering all fixtures, uses require assertions, and references Parse() which does not exist yet (TDD red phase confirmed by go vet "undefined: Parse").</done>
</task>

</tasks>

<verification>
1. `ls testdata/brocade/*.cfg | wc -l` returns 5
2. `grep -c "Defined configuration:" testdata/brocade/cfgshow_basic.cfg` returns 1
3. `grep -c "alicreate" testdata/brocade/cli_basic.cfg` returns 2
4. `grep -c "func TestParse" internal/parser/brocade/parser_test.go` returns 1
5. `go vet ./internal/parser/brocade/ 2>&1 | grep "undefined: Parse"` confirms TDD red phase
</verification>

<success_criteria>
- Five fixture files exist in testdata/brocade/ with correct Brocade cfgshow and CLI content
- parser_test.go contains table-driven tests for all five fixtures with specific IR assertions
- Tests will fail to compile because Parse() is not yet implemented (TDD red phase)
- Test structure mirrors internal/parser/mds/parser_test.go conventions (parallel, require, fixture path pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/03-brocade-parser/03-01-SUMMARY.md`
</output>
