---
phase: 02-mds-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/mds/basic.cfg
  - testdata/mds/enhanced_mode.cfg
  - testdata/mds/multi_vsan.cfg
  - testdata/mds/smart_zoning.cfg
  - testdata/mds/unsupported.cfg
  - testdata/mds/edge_cases.cfg
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
  - PARSE-05
  - PARSE-06

must_haves:
  truths:
    - "Six fixture files exist at testdata/mds/ with syntactically valid NX-OS zoning config content"
    - "basic.cfg exercises device-alias database, fcalias blocks, zone members of all three types, and a zoneset"
    - "enhanced_mode.cfg exercises zone members as 'member device-alias NAME' (NX-OS 8.5.1+ default)"
    - "multi_vsan.cfg exercises two VSANs with distinct zones sharing the same device-alias database"
    - "smart_zoning.cfg exercises 'member pwwn X init', 'member pwwn X target', 'member pwwn X both' keywords"
    - "unsupported.cfg exercises 'member interface', 'member fcid', 'member ip-address', 'member symbolic-nodename' lines"
    - "edge_cases.cfg exercises IVR zone header, empty zone, orphan zone (not in any zoneset), device-alias commit line, comment line"
  artifacts:
    - path: "testdata/mds/basic.cfg"
      provides: "PARSE-01 + PARSE-02 + PARSE-03 + PARSE-04 fixture"
      contains: "device-alias database"
    - path: "testdata/mds/enhanced_mode.cfg"
      provides: "PARSE-01 + PARSE-03 enhanced-mode fixture"
      contains: "member device-alias"
    - path: "testdata/mds/multi_vsan.cfg"
      provides: "PARSE-06 fixture"
      contains: "vsan 20"
    - path: "testdata/mds/smart_zoning.cfg"
      provides: "PARSE-03 + PARSE-05 smart-zoning fixture"
      contains: "init"
    - path: "testdata/mds/unsupported.cfg"
      provides: "PARSE-05 unsupported-member fixture"
      contains: "member interface"
    - path: "testdata/mds/edge_cases.cfg"
      provides: "IVR skip + empty zone + orphan zone fixture"
      contains: "ivr zone name"
  key_links:
    - from: "testdata/mds/basic.cfg"
      to: "internal/parser/mds/parser_test.go"
      via: "os.Open(filepath.Join('testdata', 'mds', 'basic.cfg'))"
      pattern: "filepath\\.Join.*testdata.*mds.*basic"
---

<objective>
Create the six NX-OS running-config fixture files that the MDS parser test suite will execute against. These fixtures are written BEFORE any parser code — they constitute the TDD RED-phase input that makes tests fail meaningfully, proving the parser must be built to make them pass.

Purpose: Without fixtures, the parser has no test input. Fixtures also encode the exact NX-OS syntax forms the parser must handle, serving as a concrete specification for the implementer.
Output: Six `.cfg` files in `testdata/mds/` covering all six distinct syntax categories the parser must handle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mds-parser/02-RESEARCH.md
@internal/ir/zoningconfig.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create basic.cfg and enhanced_mode.cfg fixtures</name>
  <files>testdata/mds/basic.cfg, testdata/mds/enhanced_mode.cfg</files>

  <read_first>
    - internal/ir/zoningconfig.go — understand the IR fields these fixtures must populate
    - .planning/phases/02-mds-parser/02-RESEARCH.md lines 330-370 — exact fixture content specified
  </read_first>

  <action>
Create testdata/mds/basic.cfg with the following EXACT content (exercises PARSE-01 device-alias DB, PARSE-02 fcalias, PARSE-03 all three member types, PARSE-04 zoneset):

```
device-alias database
  device-alias name Server-HBA-A pwwn 50:05:0c:00:00:c8:aa:50
  device-alias name Storage-Port-1 pwwn 50:06:0e:80:04:7c:00:01
device-alias commit

fcalias name Server-port-A vsan 10
  member pwwn 50:05:0c:00:00:c8:aa:50

zone name Server vsan 10
  member device-alias Server-HBA-A
  member fcalias Server-port-A
  member pwwn 50:05:0c:00:00:c8:aa:51

zoneset name SAN-VSAN10 vsan 10
  member Server

zoneset activate name SAN-VSAN10 vsan 10
```

Create testdata/mds/enhanced_mode.cfg with the following EXACT content (exercises PARSE-01 and PARSE-03 with NX-OS 8.5.1+ zone member syntax — member device-alias instead of member pwwn):

```
device-alias database
  device-alias name Server-HBA-A pwwn 50:05:0c:00:00:c8:aa:50
  device-alias name Storage-Port-1 pwwn 50:06:0e:80:04:7c:00:01
device-alias commit

zone name Storage-Zone vsan 10
  member device-alias Server-HBA-A
  member device-alias Storage-Port-1

zoneset name SAN-VSAN10 vsan 10
  member Storage-Zone

zoneset activate name SAN-VSAN10 vsan 10
```

Do NOT add trailing whitespace, CRLF line endings, or any comment lines. NX-OS configs use Unix line endings.
  </action>

  <verify>
    <automated>grep -c "device-alias database" /Users/fjacquet/Projects/san-conv/testdata/mds/basic.cfg && grep -c "member device-alias" /Users/fjacquet/Projects/san-conv/testdata/mds/enhanced_mode.cfg</automated>
  </verify>

  <acceptance_criteria>
    - `grep "device-alias database" testdata/mds/basic.cfg` exits 0
    - `grep "member fcalias" testdata/mds/basic.cfg` exits 0
    - `grep "member pwwn" testdata/mds/basic.cfg` exits 0
    - `grep "member device-alias Server-HBA-A" testdata/mds/enhanced_mode.cfg` exits 0
    - `grep "member pwwn" testdata/mds/enhanced_mode.cfg` exits 1 (no pwwn members in enhanced_mode)
  </acceptance_criteria>

  <done>basic.cfg and enhanced_mode.cfg exist with syntactically valid NX-OS content matching the exact text above</done>
</task>

<task type="auto">
  <name>Task 2: Create multi_vsan.cfg, smart_zoning.cfg, unsupported.cfg, and edge_cases.cfg fixtures</name>
  <files>testdata/mds/multi_vsan.cfg, testdata/mds/smart_zoning.cfg, testdata/mds/unsupported.cfg, testdata/mds/edge_cases.cfg</files>

  <read_first>
    - .planning/phases/02-mds-parser/02-RESEARCH.md lines 372-444 — exact fixture content for these four files
  </read_first>

  <action>
Create testdata/mds/multi_vsan.cfg (exercises PARSE-06: two VSANs with distinct zones, shared device-alias DB):

```
device-alias database
  device-alias name Host-A pwwn 20:00:00:00:c9:12:34:56
  device-alias name Storage-A pwwn 50:06:01:65:3e:a0:1e:d7
device-alias commit

zone name Zone-A vsan 10
  member device-alias Host-A
  member device-alias Storage-A

zoneset name ZS-VSAN10 vsan 10
  member Zone-A

zoneset activate name ZS-VSAN10 vsan 10

zone name Zone-B vsan 20
  member device-alias Host-A
  member device-alias Storage-A

zoneset name ZS-VSAN20 vsan 20
  member Zone-B

zoneset activate name ZS-VSAN20 vsan 20
```

Create testdata/mds/smart_zoning.cfg (exercises PARSE-03 + PARSE-05: member pwwn with init/target/both keywords that must be stripped):

```
zone name SmartZone vsan 10
  member pwwn 50:05:0c:00:00:c8:aa:50 init
  member pwwn 50:06:0e:80:04:7c:00:01 target
  member pwwn 50:06:0e:80:04:7c:00:02 both

zoneset name SAN-VSAN10 vsan 10
  member SmartZone
```

Create testdata/mds/unsupported.cfg (exercises PARSE-05: non-WWN member types that must be warned and skipped):

```
device-alias database
  device-alias name Server-HBA-A pwwn 50:05:0c:00:00:c8:aa:50
device-alias commit

zone name Mixed vsan 10
  member device-alias Server-HBA-A
  member interface fc1/1
  member fcid 0x6f0100 vsan 10
  member ip-address 192.168.1.1 255.255.255.0
  member symbolic-nodename myhost

zoneset name SAN-VSAN10 vsan 10
  member Mixed
```

Create testdata/mds/edge_cases.cfg (exercises IVR skip+warn, empty zone, orphan zone not in any zoneset, device-alias commit line termination, comment lines):

```
! This is a comment line — should be silently skipped
device-alias database
  device-alias name Host-A pwwn 20:00:00:00:c9:12:34:56
device-alias commit

ivr zone name IVR-CrossFabric vsan 10
  member pwwn 20:00:00:00:c9:12:34:56

zone name EmptyZone vsan 10

zone name ActiveZone vsan 10
  member device-alias Host-A

zone name OrphanZone vsan 10
  member device-alias Host-A

zoneset name SAN-VSAN10 vsan 10
  member ActiveZone

zoneset activate name SAN-VSAN10 vsan 10
```

Note on unsupported.cfg: The `member fcid 0x6f0100 vsan 10` line uses the `vsan` keyword inline — the parser's unsupported-member regex must match the entire `member fcid` prefix, not require no trailing tokens.
  </action>

  <verify>
    <automated>grep -c "vsan 20" /Users/fjacquet/Projects/san-conv/testdata/mds/multi_vsan.cfg && grep -c "init" /Users/fjacquet/Projects/san-conv/testdata/mds/smart_zoning.cfg && grep -c "member interface" /Users/fjacquet/Projects/san-conv/testdata/mds/unsupported.cfg && grep -c "ivr zone name" /Users/fjacquet/Projects/san-conv/testdata/mds/edge_cases.cfg</automated>
  </verify>

  <acceptance_criteria>
    - `grep "vsan 20" testdata/mds/multi_vsan.cfg` exits 0 (multi-VSAN present)
    - `grep "ZS-VSAN20" testdata/mds/multi_vsan.cfg` exits 0 (second zoneset present)
    - `grep "init" testdata/mds/smart_zoning.cfg` exits 0
    - `grep "target" testdata/mds/smart_zoning.cfg` exits 0
    - `grep "both" testdata/mds/smart_zoning.cfg` exits 0
    - `grep "member interface" testdata/mds/unsupported.cfg` exits 0
    - `grep "member fcid" testdata/mds/unsupported.cfg` exits 0
    - `grep "member ip-address" testdata/mds/unsupported.cfg` exits 0
    - `grep "member symbolic-nodename" testdata/mds/unsupported.cfg` exits 0
    - `grep "ivr zone name" testdata/mds/edge_cases.cfg` exits 0
    - `grep "EmptyZone" testdata/mds/edge_cases.cfg` exits 0
    - `grep "OrphanZone" testdata/mds/edge_cases.cfg` exits 0 (orphan zone present but not in zoneset member list)
    - `grep -c "member OrphanZone" testdata/mds/edge_cases.cfg` outputs 0 (orphan not in any zoneset)
  </acceptance_criteria>

  <done>All four fixture files exist; multi_vsan.cfg has exactly two VSANs; smart_zoning.cfg has all three smart-zoning keywords; unsupported.cfg has all four unsupported member types; edge_cases.cfg has IVR zone, empty zone, orphan zone, and comment line</done>
</task>

</tasks>

<verification>
Run from project root:

```bash
ls testdata/mds/*.cfg
# Expected: basic.cfg enhanced_mode.cfg multi_vsan.cfg smart_zoning.cfg unsupported.cfg edge_cases.cfg

grep "device-alias database" testdata/mds/basic.cfg
grep "member device-alias" testdata/mds/enhanced_mode.cfg
grep "vsan 20" testdata/mds/multi_vsan.cfg
grep " init" testdata/mds/smart_zoning.cfg
grep "member interface" testdata/mds/unsupported.cfg
grep "ivr zone name" testdata/mds/edge_cases.cfg
```
All greps must exit 0.
</verification>

<success_criteria>
Six NX-OS fixture files exist in testdata/mds/, each with syntactically correct content that exercises one or more distinct parsing requirements. Files are ready to be opened by parser_test.go using `os.Open(filepath.Join("..", "..", "..", "testdata", "mds", tt.fixture))`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mds-parser/02-01-SUMMARY.md`
</output>
