---
phase: 06-mds-emitter
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/emitter/mds/emitter_test.go
autonomous: true
requirements:
  - CONV-04
  - CONV-05
  - CONV-06
  - OUT-03

must_haves:
  truths:
    - "emitter_test.go compiles against package mds and references Emit(cfg, w)"
    - "go vet reports exactly 'undefined: Emit' confirming TDD red phase"
    - "Tests cover device-alias block emission (CONV-04)"
    - "Tests cover zone name X vsan N block emission (CONV-05)"
    - "Tests cover zoneset + zoneset activate emission (CONV-06)"
    - "Tests cover canonical output order: device-alias then zones then zonesets (OUT-03)"
    - "Tests verify VSAN 0 sentinel never appears literally in output"
    - "Tests verify deterministic output across repeated calls"
  artifacts:
    - path: "internal/emitter/mds/emitter_test.go"
      provides: "Complete behavioral contract for MDS Emit() via table-driven tests"
      min_lines: 250
      contains: "func TestEmit"
  key_links:
    - from: "internal/emitter/mds/emitter_test.go"
      to: "internal/ir/zoningconfig.go"
      via: "import and IR struct construction in test cases"
      pattern: "ir\\.ZoningConfig"
    - from: "internal/emitter/mds/emitter_test.go"
      to: "internal/emitter/mds/emitter.go"
      via: "Emit() call (undefined in red phase)"
      pattern: "Emit\\("
---

<objective>
Create the complete test suite for the MDS NX-OS emitter (TDD red phase).

Purpose: Define the behavioral contract for `Emit(*ir.ZoningConfig, io.Writer) error` via table-driven tests before any implementation exists. This ensures the implementation in Plan 02 is driven entirely by test expectations.

Output: `internal/emitter/mds/emitter_test.go` with 10 table-driven tests covering all 4 requirements (CONV-04, CONV-05, CONV-06, OUT-03). All tests fail with `undefined: Emit`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mds-emitter/06-RESEARCH.md

@internal/ir/zoningconfig.go
@internal/emitter/brocade/emitter_test.go
@internal/emitter/mds/doc.go

<interfaces>
<!-- IR types the executor must use to construct test inputs -->

From internal/ir/zoningconfig.go:
```go
type ZoningConfig struct {
    Aliases      map[string]*Alias
    Zones        map[string]*Zone
    ZoneConfigs  map[string]*ZoneConfig
    SourceFormat string
    Warnings     []string
}

type Alias struct {
    Name string
    PWWN string
    VSAN int
}

type Zone struct {
    Name    string
    VSAN    int
    Members []*ZoneMember
}

type ZoneMember struct {
    Type  string  // "pwwn", "alias", "unsupported"
    Value string
}

type ZoneConfig struct {
    Name      string
    VSAN      int
    ZoneNames []string
}
```

From internal/emitter/mds/doc.go:
```go
package mds
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create emitter_test.go with 10 table-driven tests covering all MDS emitter requirements</name>
  <files>internal/emitter/mds/emitter_test.go</files>
  <read_first>
    - internal/emitter/brocade/emitter_test.go (mirror this structure exactly)
    - internal/ir/zoningconfig.go (IR struct definitions)
    - internal/emitter/mds/doc.go (package name: mds)
    - .planning/phases/06-mds-emitter/06-RESEARCH.md (NX-OS output format patterns)
  </read_first>
  <behavior>
    - Test 1 (CONV-04): Given IR with 2 aliases, output contains `device-alias database`, 2x `  device-alias name X pwwn Y` lines (2-space indent), and `device-alias commit`
    - Test 2 (CONV-05 alias members): Given IR with 1 zone containing alias-type members, output contains `zone name X vsan 1` and `  member device-alias Y` lines (2-space indent)
    - Test 3 (CONV-05 pwwn members): Given IR with 1 zone containing pwwn-type members, output contains `zone name X vsan 1` and `  member pwwn Y` lines (2-space indent)
    - Test 4 (CONV-06): Given IR with 1 zoneset containing 2 zones, output contains `zoneset name X vsan 1` with `  member Z` lines AND `zoneset activate name X vsan 1` as a real (non-commented) line
    - Test 5 (OUT-03 ordering): Given full IR (aliases + zones + zoneset), device-alias block appears before zone blocks, zone blocks appear before zoneset block
    - Test 6 (VSAN 0 sentinel): Given Brocade-sourced IR with VSAN 0, all emitted lines use `vsan 1` (not `vsan 0`). Assert `vsan 0` does NOT appear in output.
    - Test 7 (empty zone skip): Given zone with all unsupported members, zone is skipped, warning appended to cfg.Warnings mentioning the zone name and "skipped", zoneset member list excludes the skipped zone
    - Test 8 (multi-VSAN passthrough): Given MDS-sourced IR with zones at VSAN 10 and VSAN 20 (composite map keys `name@vsanN`), emitter uses zone.Name (not map key), emitted output contains zone.Name without `@vsan` suffix, and uses the actual VSAN numbers
    - Test 9 (mixed members): Given zone with alias + pwwn + unsupported members, output contains `member device-alias` and `member pwwn` lines but NOT the unsupported member
    - Test 10 (deterministic): Given IR with 5 deliberately unsorted aliases and 3 zones, two consecutive Emit() calls produce byte-identical output
  </behavior>
  <action>
    Create `internal/emitter/mds/emitter_test.go` in package `mds` (NOT `mds_test` — must be same package as doc.go).

    **Mirror the Phase 5 Brocade test structure exactly:**

    1. Define helper functions (same pattern as brocade emitter_test.go):
       - `makeAlias(name, pwwn string) *ir.Alias` — returns `&ir.Alias{Name: name, PWWN: pwwn, VSAN: 0}`
       - `makeZone(name string, members ...*ir.ZoneMember) *ir.Zone` — returns with VSAN 0
       - `makeZoneVSAN(name string, vsan int, members ...*ir.ZoneMember) *ir.Zone` — returns with explicit VSAN
       - `makeZoneConfig(name string, zoneNames ...string) *ir.ZoneConfig` — returns with VSAN 0
       - `makeZoneConfigVSAN(name string, vsan int, zoneNames ...string) *ir.ZoneConfig` — returns with explicit VSAN (needed for multi-VSAN test)
       - `makeMember(typ, val string) *ir.ZoneMember`
       - `makeCfg() *ir.ZoningConfig` — returns initialized maps, SourceFormat `"brocade-fos"`, empty Warnings slice

    2. Create `func TestEmit(t *testing.T)` with `t.Parallel()`.

    3. Define table-driven tests as `tests []struct { name string; input *ir.ZoningConfig; checkFn func(t *testing.T, output string, cfg *ir.ZoningConfig) }`.
       Note: NO `scriptMode` field — MDS emitter has no script mode. The function signature is `Emit(cfg *ir.ZoningConfig, w io.Writer) error` (2 args, not 3).

    4. Implement each of the 10 tests per the `<behavior>` block above.

    **Exact expected NX-OS output formats for assertions:**

    Device-alias block (CONV-04):
    ```
    device-alias database
      device-alias name host_01 pwwn 10:00:00:00:c9:ab:cd:ef
    device-alias commit
    ```
    Use `require.Contains(t, output, "device-alias database")`, `require.Contains(t, output, "  device-alias name host_01 pwwn 10:00:00:00:c9:ab:cd:ef")` (note 2-space prefix), `require.Contains(t, output, "device-alias commit")`.

    Zone block (CONV-05):
    ```
    zone name fabric_zone1 vsan 1
      member device-alias host_01
      member pwwn 50:05:07:61:01:23:45:67
    ```
    Use `require.Contains(t, output, "zone name fabric_zone1 vsan 1")`, `require.Contains(t, output, "  member device-alias host_01")`.

    Zoneset + activate (CONV-06):
    ```
    zoneset name Production_cfg vsan 1
      member fabric_zone1

    zoneset activate name Production_cfg vsan 1
    ```
    Use `require.Contains(t, output, "zoneset name Production_cfg vsan 1")`, `require.Contains(t, output, "  member fabric_zone1")`, `require.Contains(t, output, "zoneset activate name Production_cfg vsan 1")`.

    **VSAN 0 sentinel test (Test 6):**
    Build IR with `VSAN: 0` on all structs. Assert `require.NotContains(t, output, "vsan 0")`. Assert `require.Contains(t, output, "vsan 1")`.

    **Empty zone test (Test 7):**
    Same pattern as Brocade emitter Test 8. Zone `"bad_zone"` with only unsupported members. Assert:
    - `require.NotContains(t, output, "zone name bad_zone")` (zone must not appear)
    - Warning in `cfg.Warnings` contains both `"bad_zone"` and `"skipped"`
    - If `zoneset name` appears, its member list must NOT contain `bad_zone`

    **Multi-VSAN passthrough test (Test 8):**
    Build MDS-sourced IR with `SourceFormat: "mds-nxos"`, composite map keys `"zoneA@vsan10"` and `"zoneB@vsan20"`, Zone.Name = plain `"zoneA"` and `"zoneB"`, Zone.VSAN = 10 and 20 respectively. Assert:
    - `require.Contains(t, output, "zone name zoneA vsan 10")`
    - `require.Contains(t, output, "zone name zoneB vsan 20")`
    - `require.NotContains(t, output, "@vsan")` (composite key must not leak)

    **Mixed members test (Test 9):**
    Zone with 3 members: alias "host_01", pwwn "50:05:...", unsupported "interface fc1/1". Assert:
    - `require.Contains(t, output, "  member device-alias host_01")`
    - `require.Contains(t, output, "  member pwwn 50:05:07:61:01:23:45:67")`
    - `require.NotContains(t, output, "interface fc1/1")`

    **Deterministic test (Test 10):**
    Same approach as Brocade emitter Test 10. 5 unsorted aliases ("eee", "aaa", "ddd", "bbb", "ccc"), 3 zones ("zone_c", "zone_a", "zone_b"). Run Emit twice on same IR. Assert `require.Equal(t, output, buf2.String())`.

    5. Loop over tests calling `Emit(tt.input, &buf)` (2 args). Each sub-test uses `t.Parallel()`.

    **The function call `Emit(tt.input, &buf)` will not compile** because Emit does not exist yet. This is the expected TDD red phase outcome.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/san-conv && go vet ./internal/emitter/mds/ 2>&1 | grep -q "undefined: Emit" && echo "TDD RED PHASE CONFIRMED" || echo "UNEXPECTED RESULT"</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/emitter/mds/emitter_test.go` exists with `package mds` declaration
    - File contains `func TestEmit(t *testing.T)` with 10 named subtests
    - File imports `github.com/fjacquet/san-conv/internal/ir` and `github.com/stretchr/testify/require`
    - `go vet ./internal/emitter/mds/` outputs `undefined: Emit`
    - Test names reference requirement IDs: CONV-04, CONV-05, CONV-06, OUT-03
    - No test references `scriptMode` (MDS emitter has no script mode)
    - All Emit() calls use 2-argument form: `Emit(cfg, &buf)`
    - Tests assert 2-space indentation on member lines (e.g., `"  device-alias name"`, `"  member device-alias"`, `"  member pwwn"`, `"  member "`)
    - Test 6 asserts `vsan 0` does NOT appear in output
    - Test 7 asserts cfg.Warnings contains a warning for the skipped zone
    - Test 8 asserts `@vsan` does NOT appear in output
    - Test 10 asserts byte-identical output from two consecutive Emit calls
  </acceptance_criteria>
  <done>10 table-driven tests exist defining the full MDS emitter behavioral contract. `go vet` confirms `undefined: Emit` (TDD red phase). Tests cover CONV-04, CONV-05, CONV-06, OUT-03, VSAN sentinel resolution, empty zone skip, multi-VSAN passthrough, mixed members, and deterministic output.</done>
</task>

</tasks>

<verification>
- `go vet ./internal/emitter/mds/` reports exactly `undefined: Emit` (red phase)
- `wc -l internal/emitter/mds/emitter_test.go` shows 250+ lines
- `grep -c "name:" internal/emitter/mds/emitter_test.go` shows 10 test entries
- `grep "CONV-04\|CONV-05\|CONV-06\|OUT-03" internal/emitter/mds/emitter_test.go` hits all 4 requirement IDs
- `grep "scriptMode" internal/emitter/mds/emitter_test.go` returns no matches
</verification>

<success_criteria>
- emitter_test.go exists with package mds and 10 table-driven tests
- go vet confirms undefined: Emit (TDD red phase)
- All 4 requirement IDs (CONV-04, CONV-05, CONV-06, OUT-03) are referenced in test names
- Test structure mirrors internal/emitter/brocade/emitter_test.go
- No scriptMode parameter anywhere in the test file
</success_criteria>

<output>
After completion, create `.planning/phases/06-mds-emitter/06-01-SUMMARY.md`
</output>
